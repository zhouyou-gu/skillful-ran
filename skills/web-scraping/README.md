# Web Scraping Skill

Extract structured data from public webpages with an MCP-compatible tool schema.

## Inputs

- `url` (string): target webpage URL

## Output

- `data` (array): extracted records

## Example

See `examples/basic.py`.
