# Local Lane

This stage is intentionally narrower than the E2E lane:

- one host
- split `srscucp`, `srscuup`, `srsdu`
- no core
- `test_mode` enabled
- target window: about 60 seconds

The purpose is to prove local startup discipline and capture logs, not RF or attach behavior.
