# OCUDU Build Install

Plans or executes the baseline clone and build flow inside `.local/ocudu/`.
Use dry-run first, then apply once the host audit is clean enough for build work.
See `SKILL.md`, `tool.json`, and `references/`.
