# Update Flow

- `review` writes only to `.local/ocudu/promote/`
- `promote` is for new tracked skills only
- `update` is for existing tracked skills and creates a backup under `.local/ocudu/promote/backups/`
- Full-sync update prefers the newly generated draft, so use it only when the draft is clearly better than the current tracked skill
