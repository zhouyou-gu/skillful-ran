# OCUDU Smoke Test

Runs local post-build sanity checks for build trees or installed binaries.
It is intentionally narrow: build and runtime sanity only, not RF or production claims.
See `SKILL.md`, `tool.json`, and `references/`.
