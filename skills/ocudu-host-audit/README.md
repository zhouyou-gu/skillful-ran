# OCUDU Host Audit

Checks a Linux workstation for the baseline prerequisites used by Skillful RAN.
Run it before cloning or building so missing packages and toolchain gaps are explicit.
See `SKILL.md`, `tool.json`, and `references/`.
